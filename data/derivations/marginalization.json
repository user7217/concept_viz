{
  "node_id": "marginalization",
  "grounded": true,
  "kind": "derivation",
  "statement": "The marginal distribution of a subset of random variables (here X, from a pair (X,Y)) is obtained from their joint distribution by eliminating (marginalizing out) the other variable(s) Y: sum the joint pmf over all values of Y (discrete case) or integrate the joint pdf over Y (continuous case).",
  "symbols": [
    {
      "symbol": "X, Y",
      "meaning": "random variables with a known joint distribution",
      "dimensions": "scalar or vector"
    },
    {
      "symbol": "p(x_i, y_j)",
      "meaning": "joint probability mass function of X and Y",
      "dimensions": "scalar"
    },
    {
      "symbol": "p_X(x_i)",
      "meaning": "marginal pmf of X",
      "dimensions": "scalar"
    },
    {
      "symbol": "f(x,y)",
      "meaning": "joint probability density function of X and Y",
      "dimensions": "scalar"
    },
    {
      "symbol": "f_X(x)",
      "meaning": "marginal pdf of X",
      "dimensions": "scalar"
    },
    {
      "symbol": "F(x,y)",
      "meaning": "joint cumulative distribution function",
      "dimensions": "scalar"
    },
    {
      "symbol": "p_{X|Y}(x|y)",
      "meaning": "conditional pmf/pdf of X given Y=y",
      "dimensions": "scalar"
    },
    {
      "symbol": "E_Y[\\cdot]",
      "meaning": "expectation taken over the distribution of Y",
      "dimensions": "scalar"
    },
    {
      "symbol": "X_1,\\dots,X_n",
      "meaning": "collection of random variables with a joint distribution",
      "dimensions": "scalar each"
    }
  ],
  "steps": [
    {
      "n": 1,
      "text": "Start from the known joint distribution of X and Y: discrete case p(x_i, y_j); continuous case f(x,y). This encodes the probability of X and Y taking values together, without yet discarding either variable.",
      "invokes": [],
      "cites": [
        "wikipedia:Marginal_distribution"
      ]
    },
    {
      "n": 2,
      "text": "Marginalize out Y by summing (discrete) or integrating (continuous) the joint distribution over every value Y can take, discarding Y's identity: p_X(x_i) = \\sum_j p(x_i, y_j), and symmetrically p_Y(y_j) = \\sum_i p(x_i, y_j); for continuous variables f_X(x) = \\int_c^d f(x,y)\\,dy and f_Y(y) = \\int_a^b f(x,y)\\,dx. This is literally 'summing values in a table along rows or columns and writing the sum in the margins.'",
      "invokes": [
        "algebraic_rearrangement"
      ],
      "cites": [
        "wikipedia:Marginal_distribution"
      ]
    },
    {
      "n": 3,
      "text": "Rewrite the marginalization integral as a conditional expectation. Substitute the definition of the conditional distribution, p_{X,Y}(x,y) = p_{X|Y}(x|y)\\,p_Y(y), into the integral from Step 2: p_X(x) = \\int_y p_{X|Y}(x|y)\\,p_Y(y)\\,dy. By the law of the unconscious statistician, E_Y[f(Y)] = \\int_y f(y)\\,p_Y(y)\\,dy, this integral is exactly E_Y[p_{X|Y}(x|Y)]. So marginalizing X is equivalent to averaging the conditional probability of X over all values of Y, weighted by how likely each value of Y is.",
      "invokes": [
        "conditional_probability",
        "expectation"
      ],
      "cites": [
        "wikipedia:Marginal_distribution"
      ]
    },
    {
      "n": 4,
      "text": "As a special case, this expectation form gives the transformation rule for a deterministic function X = g(Y): since p_{X|Y}(x|y) = \\delta(x - g(y)), Step 3 becomes p_X(x) = \\int_y \\delta(x - g(y))\\,p_Y(y)\\,dy.",
      "invokes": [],
      "cites": [
        "wikipedia:Marginal_distribution"
      ]
    },
    {
      "n": 5,
      "text": "Obtain the marginal CDF the same way, by evaluating the joint CDF at the edge of Y's support rather than integrating a density: if X, Y jointly take values on [a,b]\\times[c,d], then F_X(x) = F(x,d) and F_Y(y) = F(b,y); if the support is unbounded, F_X(x) = \\lim_{y\\to\\infty} F(x,y).",
      "invokes": [],
      "cites": [
        "wikipedia:Marginal_distribution"
      ]
    },
    {
      "n": 6,
      "text": "Generalize to n jointly distributed variables X_1,\\dots,X_n: the marginal of a single X_i is obtained by summing (discrete) or integrating (continuous) the joint pmf/pdf over every other variable, p_{X_i}(k) = \\sum p(x_1,\\dots,x_{i-1},k,x_{i+1},\\dots,x_n), and analogously with an (n-1)-fold integral for the continuous pdf f_{X_i}(x_i).",
      "invokes": [
        "algebraic_rearrangement"
      ],
      "cites": [
        "wikipedia:Marginal_distribution"
      ]
    }
  ],
  "properties": [],
  "assumptions": [
    {
      "assumption": "The full joint distribution over all variables (including the ones to be discarded) is known.",
      "breaks_when": "Only a conditional or partial distribution is available; then the sum/integral in Step 2 cannot be formed."
    },
    {
      "assumption": "The sum (discrete) or integral (continuous) over the discarded variable's entire support converges/exists.",
      "breaks_when": "The joint pmf/pdf is not summable or integrable over Y, e.g. an improper density with divergent tails."
    }
  ],
  "failure_modes": [
    {
      "mode": "Marginal mistaken for conditional",
      "signature": "Reported probability of X ignores that it was implicitly computed only for one value or a restricted range of Y",
      "cause": "Summing/integrating over an incomplete set of Y values (Step 2) instead of the full support of Y, e.g. treating a subset of a table as if it were the whole margin"
    },
    {
      "mode": "Undefined or infinite marginal",
      "signature": "The integral in Step 2 diverges or the sum has no finite value",
      "cause": "Joint density not integrable over Y's support, violating the convergence assumption above"
    },
    {
      "mode": "Wrong marginal CDF at finite bounds",
      "signature": "F_X(x) computed from F(x, y) at some finite y instead of the true upper bound d (or the limit as y\\to\\infty)",
      "cause": "Step 5 applied with the wrong edge of Y's support instead of its supremum"
    }
  ],
  "source_ids": [
    "wikipedia:Marginal_distribution"
  ],
  "check": {
    "node_id": "marginalization",
    "kind": "derivation",
    "grounded": true,
    "ungrounded_citations": [],
    "uncited_steps": [],
    "unknown_invocations": [],
    "closure_holes": [],
    "shape": [],
    "vacuous_invocations": false,
    "ok": true
  },
  "instantiation": {
    "node_id": "marginalization",
    "project": "agrios_ws",
    "components": [
      "ekf_global",
      "ekf_local",
      "obstaclelayer"
    ],
    "why_here": "The connection to marginalization is algorithmic rather than parameterized: the state estimation nodes ekf_global and ekf_local (from robot_localization) require this result within the Extended Kalman Filter algorithm. During the EKF prediction step, the prior distribution of the current robot state is computed by marginalizing (integrating) out the previous state from their joint distribution. Furthermore, the filter relies on the marginalization properties of multivariate Gaussians to extract marginal distributions for individual state coordinates from the full joint state distribution.",
    "concrete": [
      {
        "symbol": "X, Y",
        "in_your_project": "The continuous robot state vectors in ekf_global and ekf_local; for example, X is the current 15-dimensional state vector and Y is the previous 15-dimensional state vector that is marginalized out during the filter's prediction step."
      },
      {
        "symbol": "f(x,y)",
        "in_your_project": "The joint continuous probability density function over the current state X and previous state Y (30-dimensional joint distribution) prior to integrating out the previous state."
      },
      {
        "symbol": "f_X(x)",
        "in_your_project": "The marginal continuous probability density function of the current 15-dimensional robot state estimate (or a subset of state variables) obtained after marginalizing out Y."
      },
      {
        "symbol": "X_1,\\dots,X_n",
        "in_your_project": "The n = 15 individual random variables comprising the robot state in robot_localization: 3D position (x, y, z), orientation (roll, pitch, yaw), linear velocities (vx, vy, vz), angular velocities (vroll, vpitch, vyaw), and linear accelerations (ax, ay, az)."
      }
    ],
    "knobs": [],
    "invented": {}
  }
}
